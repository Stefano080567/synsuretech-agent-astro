---
let step = 0;
const steps = [
  {
    title: "Why WikiSure™ Exists",
    body: "WikiSure™ helps partners move faster by creating shared clarity and alignment."
  },
  {
    title: "Context",
    body: "This demo shows how one shared term can be aligned across partners."
  },
  {
    title: "Definition",
    body: "Definitions must be clear enough that teams can implement the same behavior."
  },
  {
    title: "Validation",
    body: "Humans stay in control while the system supports the decision."
  },
  {
    title: "Decision",
    body: "Every decision is documented, traceable, and reusable."
  }
];
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>WikiSure™ Demo</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background: #f5f7fa;
      margin: 0;
      padding: 40px;
    }
    .card {
      max-width: 640px;
      margin: auto;
      background: white;
      padding: 32px;
      border-radius: 14px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.08);
    }
    h1 {
      color: #0b4cff;
      margin-bottom: 12px;
    }
    p {
      font-size: 18px;
      line-height: 1.6;
    }
    .actions {
      display: flex;
      justify-content: space-between;
      margin-top: 32px;
    }
    button {
      padding: 10px 18px;
      font-size: 16px;
      border-radius: 10px;
      border: none;
      cursor: pointer;
    }
    .next {
      background: #0b4cff;
      color: white;
    }
    .back {
      background: #e5e7eb;
    }
  </style>
</head>

<body>
  <div class="card">
    <h1>{steps[step].title}</h1>
    <p>{steps[step].body}</p>

    <div class="actions">
      <button class="back" onClick={() => step = Math.max(step - 1, 0)}>Back</button>
      <button class="next" onClick={() => step = Math.min(step + 1, steps.length - 1)}>Next</button>
    </div>
  </div>
</body>
</html>
