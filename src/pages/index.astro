<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>WikiSure™ Demo</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <style>
      :root {
        --synsure-blue: #0a3cff;
        --text-main: #111;
        --bg-main: #ffffff;
        --bg-soft: #f5f5f7;
        --border-soft: #d2d2d7;
      }

      body {
        margin: 0;
        background: var(--bg-main);
        color: var(--text-main);
        font-family: system-ui, -apple-system, BlinkMacSystemFont,
          "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
      }

      header {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        height: 56px;
        display: flex;
        align-items: center;
        padding: 0 1.5rem;
        background: rgba(255, 255, 255, 0.9);
        backdrop-filter: blur(10px);
        border-bottom: 1px solid var(--border-soft);
        z-index: 10;
      }

      header a {
        color: var(--synsure-blue);
        text-decoration: none;
        font-weight: 500;
        font-size: 0.95rem;
      }

      main {
        max-width: 720px;
        margin: 7rem auto 4rem;
        padding: 0 1.5rem;
      }

      h1 {
        font-size: 2.3rem;
        font-weight: 600;
        margin-bottom: 1rem;
        letter-spacing: -0.02em;
      }

      p {
        font-size: 1.15rem;
        line-height: 1.65;
        margin-bottom: 1rem;
      }

      .nav {
        margin-top: 3rem;
        display: flex;
        justify-content: space-between;
      }

      button {
        font-size: 1rem;
        padding: 0.65rem 1.6rem;
        border-radius: 10px;
        border: 1px solid var(--border-soft);
        background: var(--bg-soft);
        cursor: pointer;
        transition: background 0.15s ease, opacity 0.15s ease;
      }

      button:hover:not(:disabled) {
        background: #eaeaec;
      }

      button:disabled {
        opacity: 0.4;
        cursor: not-allowed;
      }

      .step-indicator {
        margin-top: 2rem;
        font-size: 0.9rem;
        color: #666;
        text-align: center;
      }
    </style>
  </head>

  <body>
    <!-- Header -->
    <header>
      <a href="https://synsuretech.org" target="_blank" rel="noopener">
        ← Back to synsuretech.org
      </a>
    </header>

    <!-- Demo -->
    <main>
      <h1 id="title"></h1>
      <p id="text"></p>

      <div class="nav">
        <button id="back">Back</button>
        <button id="next">Next</button>
      </div>

      <div class="step-indicator" id="indicator"></div>
    </main>

    <!-- Pure Browser JS (no Astro state!) -->
    <script>
      const steps = [
        {
          title: "Why WikiSure™ Exists",
          text:
            "Modern ecosystems move fast — but speed alone is not enough. Alignment and shared understanding determine whether partners can execute reliably across organizations."
        },
        {
          title: "The Core Problem",
          text:
            "Teams often use the same terms but mean different things. These invisible semantic gaps lead to friction, delays, and hidden operational risk."
        },
        {
          title: "Embedded Insurance Example",
          text:
            "In embedded insurance, one partner’s 'activation' may mean contract creation, while another means policy issuance. WikiSure makes such meanings explicit — before integration starts."
        },
        {
          title: "The WikiSure™ Solution",
          text:
            "WikiSure creates a governed layer of shared meaning — reviewed by humans, supported by AI, and reusable across projects and partners."
        }
      ];

      let step = 0;

      const titleEl = document.getElementById("title");
      const textEl = document.getElementById("text");
      const backBtn = document.getElementById("back");
      const nextBtn = document.getElementById("next");
      const indicatorEl = document.getElementById("indicator");

      function render() {
        titleEl.textContent = steps[step].title;
        textEl.textContent = steps[step].text;

        backBtn.disabled = step === 0;
        nextBtn.disabled = step === steps.length - 1;

        indicatorEl.textContent =
          "Step " + (step + 1) + " of " + steps.length;
      }

      backBtn.onclick = () => {
        if (step > 0) {
          step--;
          render();
        }
      };

      nextBtn.onclick = () => {
        if (step < steps.length - 1) {
          step++;
          render();
        }
      };

      render();
    </script>
  </body>
</html>
