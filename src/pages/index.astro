---
/**
 * WikiSure™ – Landing Page
 * Purpose:
 * - Stable entry point
 * - NO logic
 * - NO JS
 * - Pure navigation to the demo
 */

const pageTitle = "WikiSure Semantic Engine – Astro Demo";
const description =
  "Cloud-ready semantic governance interface powered by WikiSure™.";
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{pageTitle}</title>

    <meta name="description" content={description} />

    <style>
      body {
        margin: 0;
        padding: 0;
        font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
          Roboto, sans-serif;
        background: #f3f4f6;
        color: #111827;
      }

      .wrapper {
        max-width: 960px;
        margin: 120px auto;
        padding: 0 24px;
      }

      .card {
        background: white;
        border-radius: 18px;
        padding: 36px;
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.08);
      }

      h1 {
        margin: 0 0 12px 0;
        font-size: 28px;
      }

      p {
        margin: 0 0 24px 0;
        color: #4b5563;
        line-height: 1.6;
      }

      a.demo-link {
        display: inline-block;
        margin-top: 8px;
        padding: 12px 18px;
        background: #2563eb;
        color: white;
        border-radius: 10px;
        font-weight: 600;
        text-decoration: none;
      }

      a.demo-link:hover {
        background: #1d4ed8;
      }
    </style>
  </head>

  <body>
    <div class="wrapper">
      <div class="card">
        <h1>WikiSure Semantic Engine – Astro Demo</h1>

        <p>
          This is a minimal, cloud-ready demo of the WikiSure™ Semantic
          Governance Engine.
        </p>

        <p>
          The demo illustrates how a platform provider can evaluate and align
          shared definitions in embedded insurance using a human-in-the-loop
          governance flow.
        </p>

        <!-- CRITICAL: pure link, no JS -->
        <a class="demo-link" href="/demo">
          Open Demo
        </a>
      </div>
    </div>
  </body>
</html>
