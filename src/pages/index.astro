<style>
  :root {
    --synsure-blue: #0A2FFF;
    --text-dark: #0f172a;
    --text-muted: #475569;
    --bg-light: #f8fafc;
  }

  body {
    margin: 0;
    font-family: -apple-system, BlinkMacSystemFont,
      "SF Pro Text", "SF Pro Display",
      "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
    background: var(--bg-light);
    color: var(--text-dark);
  }

  main {
    max-width: 820px;
    margin: 0 auto;
    padding: 64px 24px;
  }

  h1, h2 {
    font-weight: 600;
    letter-spacing: -0.02em;
  }

  h1 {
    font-size: 42px;
    margin-bottom: 12px;
  }

  h2 {
    font-size: 28px;
    margin-bottom: 16px;
  }

  p {
    font-size: 18px;
    line-height: 1.6;
    color: var(--text-muted);
  }

  button {
    margin-top: 32px;
    padding: 14px 28px;
    font-size: 16px;
    font-weight: 500;
    border-radius: 10px;
    border: none;
    cursor: pointer;
    background: var(--synsure-blue);
    color: white;
  }

  button:hover {
    opacity: 0.92;
  }
</style>
