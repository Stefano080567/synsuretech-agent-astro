---
/**
 * WikiSure™ Demo – Step 1: WHY
 * Pure UI. No JS frameworks. No Tailwind. Demo-freeze safe.
 */
const title = "WikiSure™ Demo — Why";
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>

    <style>
      :root {
        --bg: #f5f5f7;
        --card: #ffffff;
        --text: #1d1d1f;
        --muted: #6e6e73;
        --accent: #0071e3;
        --radius: 18px;
      }

      * {
        box-sizing: border-box;
        font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text",
          "Segoe UI", Helvetica, Arial, sans-serif;
      }

      body {
        margin: 0;
        background: var(--bg);
        color: var(--text);
      }

      .wrapper {
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 40px 20px;
      }

      .card {
        background: var(--card);
        max-width: 880px;
        width: 100%;
        padding: 64px;
        border-radius: var(--radius);
        box-shadow: 0 30px 60px rgba(0, 0, 0, 0.08);
      }

      .step {
        font-size: 14px;
        letter-spacing: 0.08em;
        color: var(--muted);
        margin-bottom: 12px;
        text-transform: uppercase;
      }

      h1 {
        font-size: 44px;
        line-height: 1.15;
        margin: 0 0 24px 0;
        font-weight: 600;
      }

      p {
        font-size: 20px;
        line-height: 1.6;
        color: var(--muted);
        margin-bottom: 28px;
      }

      .highlight {
        color: var(--text);
        font-weight: 500;
      }

      .actions {
        margin-top: 40px;
      }

      .btn {
        display: inline-block;
        background: var(--accent);
        color: white;
        padding: 14px 28px;
        border-radius: 999px;
        font-size: 18px;
        text-decoration: none;
        font-weight: 500;
        transition: transform 0.15s ease, box-shadow 0.15s ease;
      }

      .btn:hover {
        transform: translateY(-1px);
        box-shadow: 0 8px 20px rgba(0, 113, 227, 0.35);
      }

      .footer {
        margin-top: 48px;
        font-size: 14px;
        color: var(--muted);
      }
    </style>
  </head>

  <body>
    <main class="wrapper">
      <section class="card">
        <div class="step">Step 1 of 6 — Why</div>

        <h1>Why WikiSure™ exists</h1>

        <p>
          Modern ecosystems rarely fail because of missing technology.
          They fail because <span class="highlight">shared understanding is missing</span>.
        </p>

        <p>
          Platforms, insurers, partners and regulators often use the same words —
          but mean different things. Decisions slow down, responsibility becomes
          unclear, and AI systems amplify the confusion.
        </p>

        <p>
          WikiSure™ introduces a <span class="highlight">governed semantic layer</span>:
          definitions are explicit, assumptions are visible, and decisions remain
          traceable.
        </p>

        <p>
          AI can assist. Humans stay accountable.
        </p>

        <div class="actions">
          <!-- Next step will be /demo/define -->
          <a class="btn" href="/demo/define">
            Start the demo →
          </a>
        </div>

        <div class="footer">
          This demo shows one moment in a governed decision flow.
        </div>
      </section>
    </main>
  </body>
</html>
