---
---
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>WikiSure Agent Demo</title>

    <style>
      body {
        font-family: system-ui, -apple-system, BlinkMacSystemFont;
        background: #f8f9fb;
        margin: 0;
        padding: 64px 24px;
        color: #111;
      }

      .screen {
        display: none;
        max-width: 800px;
        margin: 0 auto 48px;
        background: white;
        padding: 32px;
        border-radius: 14px;
        box-shadow: 0 6px 18px rgba(0,0,0,0.08);
      }

      .screen.active {
        display: block;
      }

      .step-label {
        font-size: 12px;
        text-transform: uppercase;
        letter-spacing: 0.08em;
        color: #555;
        margin-bottom: 8px;
      }

      h1 {
        margin-top: 0;
        margin-bottom: 12px;
      }

      p {
        margin: 12px 0 24px;
        color: #555;
      }

      button {
        padding: 12px 18px;
        border-radius: 10px;
        border: none;
        background: #004fc4;
        color: white;
        font-size: 16px;
        cursor: pointer;
      }
    </style>
  </head>

  <body>

    <!-- SCREEN 1 -->
    <section class="screen active" data-step="1">
      <div class="step-label">Step 1 of 5</div>
      <h1>Draft a Meaning</h1>
      <p>Create an initial definition everyone can react to.</p>
      <button onclick="nextStep()">Next</button>
    </section>

    <!-- SCREEN 2 -->
    <section class="screen" data-step="2">
      <div class="step-label">Step 2 of 5</div>
      <h1>Validate Meaning</h1>
      <p>Check logic, consistency and risks.</p>
      <button onclick="nextStep()">Next</button>
    </section>

    <!-- SCREEN 3 -->
    <section class="screen" data-step="3">
      <div class="step-label">Step 3 of 5</div>
      <h1>Add Perspectives</h1>
      <p>OEM, Insurer and Platform perspectives are considered.</p>
      <button onclick="nextStep()">Next</button>
    </section>

    <!-- SCREEN 4 -->
    <section class="screen" data-step="4">
      <div class="step-label">Step 4 of 5</div>
      <h1>AI can suggest.<br/>But it can’t decide.</h1>
      <p>Without agreement, definitions remain opinions.</p>
      <button onclick="nextStep()">Next</button>
    </section>

    <!-- SCREEN 5 -->
    <section class="screen" data-step="5">
      <div class="step-label">Step 5 of 5</div>
      <h1>Shared Meaning</h1>
      <p>This is what can be reused, trusted and referenced.</p>
      <button onclick="nextStep()">Restart</button>
    </section>

    <script is:inline>
      let currentStep = 1;

      window.nextStep = () => {
        // hide current
        const current = document.querySelector(`[data-step="${currentStep}"]`);
        if (current) current.classList.remove("active");

        // calculate next
        currentStep++;
        if (currentStep > 5) currentStep = 1;

        // show next
        const next = document.querySelector(`[data-step="${currentStep}"]`);
        if (next) next.classList.add("active");
      }
    </script>

  </body>
</html>
