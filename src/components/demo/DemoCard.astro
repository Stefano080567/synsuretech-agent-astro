---
/**
 * DemoCard.astro
 * Reine UI-Komponente für die WikiSure Demo.
 * KEINE Logik, KEINE API-Aufrufe, KEIN Zustand außerhalb des DOM.
 */
---

<style>
  .card {
    max-width: 980px;
    margin: 0 auto;
    background: white;
    border-radius: 18px;
    padding: 36px;
    box-shadow: 0 20px 40px rgba(0,0,0,0.08);
  }

  h1 { margin: 0 0 6px 0; }
  .subtle { color: #6b7280; margin-bottom: 24px; line-height: 1.5; }

  label { display: block; font-weight: 600; margin-bottom: 6px; }
  input, textarea {
    width: 100%;
    padding: 10px;
    border-radius: 10px;
    border: 1px solid #d1d5db;
    margin-bottom: 12px;
    font-size: 14px;
  }

  textarea { min-height: 120px; }

  .grid2 {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 14px;
    margin-top: 10px;
  }

  button {
    margin-top: 14px;
    padding: 10px 14px;
    border-radius: 10px;
    border: none;
    background: #2563eb;
    color: white;
    font-weight: 700;
    cursor: pointer;
  }

  .hint {
    font-size: 12px;
    color: #6b7280;
    margin-top: 8px;
  }

  #decision {
    margin-top: 24px;
    padding: 16px;
    border-radius: 12px;
    background: #f9fafb;
    border: 1px solid #e5e7eb;
  }
</style>

<div class="card">
  <h1>WikiSure Demo</h1>
  <p class="subtle">
    A simple single-user semantic governance demo. Enter a term and evaluate a proposed definition.
  </p>

  <!-- Step 1 -->
  <div>
    <label>Term</label>
    <input id="term" placeholder="e.g. Shared Definition" />
    <button id="aiSuggestBtn">AI Suggest</button>
  </div>

  <!-- Step 2 -->
  <div class="grid2">
    <div>
      <label>Canonical Definition (Baseline)</label>
      <textarea id="canonical" placeholder="Canonical definition of the term"></textarea>
    </div>
    <div>
      <label>Proposed Change (Draft)</label>
      <textarea id="draft" placeholder="Proposed changes to the definition"></textarea>
    </div>
  </div>

  <div class="hint">AI suggestions will be displayed here.</div>

  <button id="governanceBtn">Run Governance Check</button>

  <div id="decision" style="display:none;">
    <h3>Governance Decision</h3>
    <div id="decisionContent"></div>
  </div>
</div>
