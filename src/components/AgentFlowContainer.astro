---
/*
  WikiSure Demo – Stable Flow Container
  Purpose: Simple, robust, non-reactive demo flow
  No external state, no agents, no Lovable hooks
*/

const steps = [
  {
    title: "Why WikiSure™ Exists",
    body: `
      <p>
        Modern ecosystems move fast — but speed alone is not enough.
        Alignment, clarity and shared understanding determine whether
        partners can execute reliably across organizations.
      </p>
      <p>
        WikiSure™ makes shared meaning explicit — governed, reusable,
        and trusted across partners.
      </p>
    `,
  },
  {
    title: "Context",
    body: `
      <p>
        This demo shows how a shared definition can be aligned
        across insurers, platforms and distributors.
      </p>
      <p>
        Humans stay in control. AI assists — never decides.
      </p>
    `,
  },
  {
    title: "Definition",
    body: `
      <p><strong>Proposed definition:</strong></p>
      <p>
        Consent is explicitly requested at the moment of activation,
        recorded with scope and timestamp, and shared with all
        involved parties.
      </p>
    `,
  },
  {
    title: "Decision",
    body: `
      <p>
        The definition is accepted and becomes reusable
        across integrations and partners.
      </p>
    `,
  },
];

let index = 0;
const total = steps.length;
---

<section class="wrapper">
  <div class="card">
    <h2>{steps[index].title}</h2>
    <div set:html={steps[index].body} />

    <div class="nav">
      <button
        disabled={index === 0}
        onClick={() => (index = Math.max(0, index - 1))}
      >
        Back
      </button>

      <div class="dots">
        {steps.map((_, i) => (
          <span class={i === index ? "dot active" : "dot"} />
        ))}
      </div>

      <button
        disabled={index === total - 1}
        onClick={() => (index = Math.min(total - 1, index + 1))}
      >
        Next
      </button>
    </div>
  </div>

  <a class="exit" href="https://synsuretech.org" target="_self">
    ← Back to synsuretech.org
  </a>
</section>

<style>
  :root {
    --blue: #2563eb;
    --gray: #6b7280;
    --bg: #f9fafb;
  }

  body {
    margin: 0;
    background: var(--bg);
    font-family: -apple-system, BlinkMacSystemFont,
      "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
  }

  .wrapper {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 24px;
  }

  .card {
    background: white;
    width: 560px;
    padding: 32px;
    border-radius: 14px;
    box-shadow: 0 10px 30px rgba(0,0,0,0.08);
  }

  h2 {
    margin-top: 0;
    font-size: 22px;
  }

  p {
    font-size: 16px;
    line-height: 1.6;
    color: #111;
  }

  .nav {
    margin-top: 28px;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  button {
    background: var(--blue);
    color: white;
    border: none;
    padding: 10px 18px;
    border-radius: 999px;
    font-size: 14px;
    cursor: pointer;
  }

  button[disabled] {
    background: #c7d2fe;
    cursor: not-allowed;
  }

  .dots {
    display: flex;
    gap: 8px;
  }

  .dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: #e5e7eb;
  }

  .dot.active {
    background: var(--blue);
  }

  .exit {
    font-size: 14px;
    color: var(--gray);
    text-decoration: none;
  }

  .exit:hover {
    text-decoration: underline;
  }
</style>
