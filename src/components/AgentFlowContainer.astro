---
/*
  ⚠️ DEMO-FROZEN FILE
  Client-side only.
  No external state.
*/
---

<style>
  :root {
    --blue: #2563eb;
    --text: #0f172a;
    --soft: #f8fafc;
    --border: #e5e7eb;
  }

  .container {
    max-width: 880px;
    margin: 80px auto;
    padding: 40px;
    background: white;
    border-radius: 18px;
    box-shadow: 0 20px 40px rgba(0,0,0,.06);
    font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", Inter, sans-serif;
  }

  h2 { font-size: 28px; margin-bottom: 16px; }
  p  { font-size: 18px; line-height: 1.6; }

  .definition-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 32px;
    margin-top: 24px;
  }

  .box {
    padding: 24px;
    border-radius: 14px;
    background: var(--soft);
    border: 1px solid var(--border);
  }

  .box.highlight {
    border-color: var(--blue);
  }

  .nav {
    display: flex;
    justify-content: space-between;
    margin-top: 48px;
  }

  button {
    padding: 12px 24px;
    border-radius: 999px;
    border: none;
    font-size: 16px;
    cursor: pointer;
  }

  .primary { background: var(--blue); color: white; }
  .secondary { background: #e5e7eb; color: #475569; }

  button:disabled { opacity: .4; cursor: default; }

  .exit {
    margin-top: 32px;
    text-align: center;
  }

  .exit a {
    font-size: 14px;
    color: var(--blue);
    text-decoration: none;
  }
</style>

<div class="container">
  <div id="content"></div>

  <div class="nav">
    <button id="back" class="secondary">Back</button>
    <button id="next" class="primary">Next</button>
  </div>

  <div class="exit">
    <a href="https://synsuretech.org" target="_blank" rel="noopener">
      ← Back to SynsureTech.org
    </a>
  </div>
</div>

<script client:load>
  const steps = [
    `
      <h2>Why WikiSure™ Exists</h2>
      <p>
        Modern ecosystems move fast — but speed alone is not enough.
        Alignment and clarity enable execution.
      </p>
      <p>
        AI assists — humans stay in the driver’s seat.
      </p>
    `,
    `
      <h2>Demo Context</h2>
      <p>
        This demo shows a governed definition flow across partners.
      </p>
    `,
    `
      <h2>Term Selection</h2>
      <p>
        One shared term. Multiple interpretations.
      </p>
    `,
    `
      <h2>Definition</h2>
      <div class="definition-grid">
        <div class="box">
          <h3>Current Definition</h3>
          <p>Consent is collected during onboarding.</p>
        </div>
        <div class="box highlight">
          <h3>Proposed Definition</h3>
          <p>Consent is explicitly requested and traceable.</p>
        </div>
      </div>
    `,
    `
      <h2>Validation</h2>
      <p>
        Differences are reviewed before execution.
      </p>
    `,
    `
      <h2>Decision</h2>
      <p>
        One shared definition. Clear accountability.
      </p>
    `
  ];

  let step = 0;

  const content = document.getElementById("content");
  const back = document.getElementById("back");
  const next = document.getElementById("next");

  function render() {
    content.innerHTML = steps[step];
    back.disabled = step === 0;
    next.disabled = step === steps.length - 1;
  }

  back.onclick = () => { step--; render(); };
  next.onclick = () => { step++; render(); };

  render();
</script>
