---
/*
  ⚠️ DEMO-FROZEN FILE
  This file is intentionally isolated.
  DO NOT auto-generate, refactor, or replace.
  Any change must be explicit and manual.
*/

const READ_ONLY = true;

const steps = [
  {
    title: "Why WikiSure™ Exists",
    body: `
      <h2>Why WikiSure™ Exists</h2>
      <p>
        Modern ecosystems move fast — but speed alone is not enough.
        Alignment and shared understanding determine whether partners
        can execute reliably across organizations.
      </p>
      <p>
        WikiSure™ exists to make meaning explicit, decisions traceable,
        and collaboration repeatable — with humans staying in control.
      </p>
    `
  },
  {
    title: "Demo Context",
    body: `
      <h2>Demo Context</h2>
      <p>
        This demo shows a single, focused moment:
        one shared term, one decision, one accountable outcome.
      </p>
      <ul>
        <li><strong>Market:</strong> Germany</li>
        <li><strong>Scenario:</strong> Embedded Insurance</li>
        <li><strong>Perspective:</strong> Platform Provider</li>
      </ul>
    `
  },
  {
    title: "Term Selection",
    body: `
      <h2>Term Selection</h2>
      <p>
        Choose a term that multiple partners use — but may interpret differently.
      </p>
      <p>
        Example: <strong>Embedded Insurance Consent</strong>
      </p>
      <p>
        This is where alignment usually starts to break — or becomes governed.
      </p>
    `
  },
  {
    title: "Definition",
    body: `
      <h2>Definition</h2>
      <div class="definitionGrid">
        <div class="definitionBox">
          <h3>Current Definition</h3>
          <p>
            Consent is obtained somewhere during the customer journey.
          </p>
        </div>

        <div class="definitionBox highlighted">
          <h3>Proposed Definition</h3>
          <p>
            Consent is explicitly collected before any insurance-related
            data is shared or processed, with a clear audit trail.
          </p>
        </div>
      </div>
    `
  },
  {
    title: "Validation",
    body: `
      <h2>Validation</h2>
      <p>
        The system supports the review — but a human owns the decision.
      </p>
      <ul>
        <li>Does this introduce ambiguity?</li>
        <li>Does responsibility shift?</li>
        <li>Does this create compliance risk?</li>
      </ul>
      <p>
        Outcome signals: <strong>PASS · REVISE · FLAG</strong>
      </p>
    `
  },
  {
    title: "Decision",
    body: `
      <h2>Decision</h2>
      <p>
        A governed outcome is more than yes or no.
      </p>
      <p>
        It captures the decision, the reasoning, and the next action —
        so teams can implement faster and with confidence.
      </p>
      <p>
        This is how shared understanding turns into execution.
      </p>
    `
  }
];
---

<div class="agentFlow">
  <div id="stepContainer" class="stepContainer"></div>

  <div class="nav">
    <button id="prevBtn">Back</button>
    <span id="stepIndicator"></span>
    <button id="nextBtn">Next</button>
  </div>
</div>

<script>
  let currentStep = 0;
  const steps = JSON.parse(JSON.stringify({steps}));

  const container = document.getElementById("stepContainer");
  const indicator = document.getElementById("stepIndicator");
  const prevBtn = document.getElementById("prevBtn");
  const nextBtn = document.getElementById("nextBtn");

  function renderStep() {
    const step = steps[currentStep];
    container.innerHTML = step.body;
    indicator.textContent = `Step ${currentStep + 1} of ${steps.length}`;

    prevBtn.disabled = currentStep === 0;
    nextBtn.disabled = currentStep === steps.length - 1;
  }

  prevBtn.addEventListener("click", () => {
    if (currentStep > 0) {
      currentStep--;
      renderStep();
    }
  });

  nextBtn.addEventListener("click", () => {
    if (currentStep < steps.length - 1) {
      currentStep++;
      renderStep();
    }
  });

  renderStep();
</script>

<style>
  .agentFlow {
    max-width: 860px;
    margin: 0 auto;
    padding: 48px 24px;
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  }

  h2 {
    font-size: 28px;
    margin-bottom: 16px;
  }

  h3 {
    font-size: 18px;
    margin-bottom: 8px;
  }

  p, li {
    font-size: 16px;
    line-height: 1.6;
    color: #333;
  }

  ul {
    padding-left: 20px;
  }

  .definitionGrid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 24px;
    margin-top: 24px;
  }

  @media (min-width: 700px) {
    .definitionGrid {
      grid-template-columns: 1fr 1fr;
    }
  }

  .definitionBox {
    padding: 20px;
    border-radius: 12px;
    background: #f5f5f7;
  }

  .definitionBox.highlighted {
    background: #eef4ff;
    border: 1px solid #c7dcff;
  }

  .nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: 40px;
  }

  button {
    padding: 10px 18px;
    border-radius: 999px;
    border: none;
    background: #000;
    color: #fff;
    cursor: pointer;
    font-size: 14px;
  }

  button:disabled {
    opacity: 0.3;
    cursor: not-allowed;
  }

  #stepIndicator {
    font-size: 14px;
    color: #666;
  }
</style>
