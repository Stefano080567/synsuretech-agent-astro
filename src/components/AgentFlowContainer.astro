---
const steps = [
  {
    title: "Why WikiSure™ Exists",
    body: `
      <h2>Why WikiSure™ Exists</h2>
      <p>
        Modern ecosystems move fast — but speed alone is not enough.
        Alignment, clarity and shared understanding determine whether
        partners can execute reliably across organizations.
      </p>
      <p>
        WikiSure™ exists to make meaning explicit.
        AI can assist — but humans stay in the driver’s seat.
      </p>
    `
  },
  {
    title: "Demo Context",
    body: `
      <h2>Demo Context</h2>
      <p>
        This demo shows a single governed definition across multiple partners
        in an embedded insurance ecosystem.
      </p>
      <p>
        Perspective: Platform Provider<br/>
        Region: Germany<br/>
        Scope: One term, one decision
      </p>
    `
  },
  {
    title: "Term Selection",
    body: `
      <h2>Term Selection</h2>
      <p>
        Partners often use the same term — but mean different things.
      </p>
      <p><strong>Selected term:</strong> Consent</p>
      <p>
        WikiSure focuses on terms that directly affect responsibility,
        compliance and execution.
      </p>
    `
  },
  {
    title: "Definition",
    body: `
      <h2>Step 4: Definition</h2>
      <div class="grid">
        <div class="card">
          <h4>Current Definition</h4>
          <p>
            Consent is collected during the user onboarding process
            and stored by the platform provider for later insurance activation.
          </p>
        </div>
        <div class="card">
          <h4>Proposed Definition</h4>
          <p>
            Consent is explicitly requested at the moment of insurance activation,
            recorded with timestamp and scope, and shared with all involved parties
            to ensure traceability and accountability.
          </p>
        </div>
      </div>
    `
  },
  {
    title: "Validation",
    body: `
      <h2>Validation</h2>
      <p>
        Before a definition becomes operational, it is validated.
      </p>
      <ul>
        <li>Is the definition unambiguous?</li>
        <li>Are responsibilities clearly assigned?</li>
        <li>Does it introduce regulatory or compliance risk?</li>
      </ul>
      <p>
        AI can assist — but a human reviewer owns the decision.
      </p>
    `
  },
  {
    title: "Decision",
    body: `
      <h2>Decision</h2>
      <p>
        The outcome is not just “approved” or “rejected”.
      </p>
      <p>
        A governed decision includes:
      </p>
      <ul>
        <li>The agreed definition</li>
        <li>The validation signal</li>
        <li>The rationale behind the decision</li>
      </ul>
      <p>
        This shared understanding enables faster, safer execution across partners.
      </p>
    `
  }
];
---

<div id="flow">
  {steps.map((step, i) => (
    <section class="step" data-step={i}>
      <div class="card-wrapper" set:html={step.body} />
    </section>
  ))}

  <div class="nav">
    <button id="back">Back</button>
    <button id="next">Next</button>
  </div>

  <div class="dots">
    {steps.map((_, i) => (
      <span class="dot" data-dot={i}></span>
    ))}
  </div>
</div>

<script is:inline>
  let current = 0;
  const steps = document.querySelectorAll(".step");
  const dots = document.querySelectorAll(".dot");
  const back = document.getElementById("back");
  const next = document.getElementById("next");

  function update() {
    steps.forEach((s, i) =>
      s.style.display = i === current ? "block" : "none"
    );
    dots.forEach((d, i) =>
      d.classList.toggle("active", i === current)
    );
    back.disabled = current === 0;
    next.disabled = current === steps.length - 1;
  }

  back.onclick = () => {
    if (current > 0) current--;
    update();
  };

  next.onclick = () => {
    if (current < steps.length - 1) current++;
    update();
  };

  update();
</script>

<style>
#flow {
  max-width: 900px;
  margin: 80px auto;
  padding: 0 24px;
}

.step {
  display: none;
}

.grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 24px;
}

.card, .card-wrapper {
  padding: 28px;
  border-radius: 18px;
  background: #f7f7f8;
}

.nav {
  display: flex;
  justify-content: space-between;
  margin-top: 48px;
}

button {
  padding: 10px 22px;
  border-radius: 999px;
  border: none;
  background: #2563eb;
  color: white;
}

button:disabled {
  opacity: 0.35;
}

.dots {
  display: flex;
  justify-content: center;
  gap: 8px;
  margin-top: 24px;
}

.dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: #ddd;
}

.dot.active {
  background: #2563eb;
}
</style>
