---
/**
 * AgentFlowContainer.astro
 * Demo-only step controller (UI only)
 */
---

<style>
  .step { display: none; }
  .step.active { display: block; }

  .nav {
    margin-top: 32px;
    display: flex;
    justify-content: space-between;
  }

  button {
    padding: 10px 16px;
    border-radius: 10px;
    border: none;
    background: #2563eb;
    color: white;
    font-weight: 600;
    cursor: pointer;
  }

  button.secondary {
    background: #e5e7eb;
    color: #111827;
  }
</style>

<div id="flow">

  <section class="step active">
    <h2>Why WikiSure™</h2>
    <p>Shared understanding before automation.</p>
  </section>

  <section class="step">
    <h2>Definition</h2>
    <p>Terms become explicit and governed.</p>
  </section>

  <section class="step">
    <h2>Validation</h2>
    <p>Humans stay accountable, AI assists.</p>
  </section>

  <section class="step">
    <h2>Decision</h2>
    <p>Outcomes are traceable and explainable.</p>
  </section>

  <section class="step">
    <h2>Impact</h2>
    <p>Less friction across ecosystems.</p>
  </section>

  <section class="step">
    <h2>Done</h2>
    <p>This is one moment in a larger system.</p>
  </section>

  <div class="nav">
    <button id="prev" class="secondary">Back</button>
    <button id="next">Next</button>
  </div>
</div>

<script>
  const steps = document.querySelectorAll(".step");
  let current = 0;

  const show = (i) => {
    steps.forEach((s, idx) => {
      s.classList.toggle("active", idx === i);
    });
  };

  document.getElementById("next").onclick = () => {
    if (current < steps.length - 1) current++;
    show(current);
  };

  document.getElementById("prev").onclick = () => {
    if (current > 0) current--;
    show(current);
  };
</script>
