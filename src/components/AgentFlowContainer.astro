---
/*
  ⚠️ DEMO-FROZEN FILE
  This file is intentionally isolated.
  DO NOT auto-generate, refactor, or replace.
*/

const READ_ONLY = true;

const steps = [
  {
    title: "Why WikiSure™ Exists",
    body: `
      <p>
        Modern ecosystems move fast.
        What makes them succeed is not more technology,
        but shared clarity across partners.
      </p>
      <p>
        WikiSure™ creates explicit, governed definitions
        so teams can move faster — together.
      </p>
    `
  },
  {
    title: "Demo Context",
    body: `
      <p>
        This demo shows a single decision flow
        from a platform provider perspective.
      </p>
      <p>
        One term. One definition. One clear outcome.
      </p>
    `
  },
  {
    title: "Term Selection",
    body: `
      <p>
        A shared term is selected that multiple parties use.
      </p>
      <p>
        Example: <strong>Embedded Insurance Consent</strong>
      </p>
    `
  },
  {
    title: "Definition",
    body: `
      <p>
        A baseline definition is compared with a refined proposal.
      </p>
      <p>
        If two teams cannot implement the same behavior,
        the definition is not ready.
      </p>
    `
  },
  {
    title: "Validation",
    body: `
      <p>
        The definition is reviewed for clarity,
        responsibility and risk.
      </p>
      <p>
        Humans stay accountable.
        The system assists — it does not decide.
      </p>
    `
  },
  {
    title: "Decision",
    body: `
      <p>
        A governed decision is recorded
        with rationale and next steps.
      </p>
      <p>
        Shared understanding becomes executable.
      </p>
    `
  }
];
---

<style>
  @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap');

  .flow-wrapper {
    font-family: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
    max-width: 720px;
    margin: 80px auto;
    padding: 0 24px;
    color: #0f172a;
  }

  h1 {
    font-size: 28px;
    font-weight: 600;
    margin-bottom: 16px;
  }

  .step-body p {
    font-size: 16px;
    line-height: 1.6;
    margin-bottom: 12px;
  }

  .nav {
    display: flex;
    justify-content: space-between;
    margin-top: 40px;
  }

  button {
    font-family: inherit;
    font-size: 14px;
    padding: 10px 18px;
    border-radius: 8px;
    border: 1px solid #1e40af;
    background: #1e40af;
    color: white;
    cursor: pointer;
  }

  button[disabled] {
    opacity: 0.4;
    cursor: not-allowed;
  }
</style>

<div class="flow-wrapper">
  <h1 id="step-title"></h1>
  <div class="step-body" id="step-body"></div>

  <div class="nav">
    <button id="prevBtn">Back</button>
    <button id="nextBtn">Next</button>
  </div>
</div>

<script>
  let index = 0;

  const titleEl = document.getElementById("step-title");
  const bodyEl = document.getElementById("step-body");
  const prevBtn = document.getElementById("prevBtn");
  const nextBtn = document.getElementById("nextBtn");

  function render() {
    titleEl.innerText = steps[index].title;
    bodyEl.innerHTML = steps[index].body;

    prevBtn.disabled = index === 0;
    nextBtn.disabled = index === steps.length - 1;
  }

  prevBtn.addEventListener("click", () => {
    if (index > 0) {
      index--;
      render();
    }
  });

  nextBtn.addEventListener("click", () => {
    if (index < steps.length - 1) {
      index++;
      render();
    }
  });

  render();
</script>
